# Python
__pycache__/
*.pyc
.venv/
venv/

# Third-party Lua lib is downloaded at runtime (MIT, by Ross Nichols) — not redistributed here
LibSerialize.lua

# Build intermediates
*.ser

# Never commit game/account data
WTF/
*BugGrabber*.lua

# Private project documentation (admin only)
CLAUDE.md

# WA iterations folder (testing versions) — skrypty build/test sa tracked
_iterations/*
!_iterations/build_twist_queue.py
!_iterations/test_queue_behavior.py
